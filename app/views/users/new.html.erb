<h1>新規投稿</h1>
<%= form_with model: @user, local: true do |f| %>
  <div>
    <%= f.label :name, "お名前" %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :age, "年齢：" %>
    <%= f.text_field :age %>
  </div>
  <div>
    <%= f.submit "送信" %>
  </div>

<% end %>
